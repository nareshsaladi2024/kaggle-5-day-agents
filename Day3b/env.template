# Google AI/Vertex AI Configuration
GOOGLE_API_KEY=your-google-api-key-here
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Agent Configuration
AGENT_MODEL=gemini-2.5-flash-lite
APP_NAME=MemoryDemoApp
USER_ID=demo_user

# Session Service Configuration
SESSION_SERVICE_TYPE=inmemory
# Options: inmemory (temporary) or database (persistent)
DB_URL=sqlite:///memory_data.db

# Memory Configuration
MEMORY_MODE=reactive
# Options: reactive (load_memory) or proactive (preload_memory)
# - reactive: Agent decides when to search memory (more efficient)
# - proactive: Automatically loads memory before every turn (guaranteed context)


